# Copy this file to `.env` and fill in your values.
# NOTE: reposync reads these from the process environment. The CLI does not
# auto-load .env, so export/set them in your shell before running (see README
# "Development"). Your real .env is git-ignored and reposync-ignored.

# ngrok auth token — required only when using the default ngrok tunnel.
# Get a free one at https://dashboard.ngrok.com/get-started/your-authtoken

NGROK_AUTHTOKEN=