# Copy this file to `.env` and fill in your values.
# The CLI auto-loads .env from the repo directory on startup —
# no need to manually export variables in your shell.

# ngrok auth token — required only when using the default ngrok tunnel.
# Get a free one at https://dashboard.ngrok.com/get-started/your-authtoken

NGROK_AUTHTOKEN=